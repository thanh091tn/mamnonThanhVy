# API base URL (default in dev: Vite proxy → http://localhost:3000)
# VITE_API_BASE=/api

# Show "Quản lý" on signup when true — must match backend ENABLE_MANAGER_REGISTER
# VITE_ENABLE_MANAGER_REGISTER=false
